import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  goalContainer: {
    padding: 10,
    marginVertical: 5,
    backgroundColor: "#ccc",
    borderColor: "black",
    borderWidth: 1,
  },
});
